    <h1><%= @nickname %>の投稿一覧❤️</h1>
    
    <table>
      <tr>
        <th>ニックネーム</th>
        <th>タイトル</th>
        <th>内容</th>
      </tr>

    <% @posts.each do |post| %>
      <tr>
        <td><%= post.user.nickname %></td>
        <td><%= post.title %></td>
        <td><%= post.text %></td>
      </tr>
    <% end %>
    </table>

        <%= link_to '戻る', posts_path %>